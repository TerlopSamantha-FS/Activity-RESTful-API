<form method="POST" action="/products/{% if products.id %}{{ products.id }}{% endif %}">
    {% if products.id %}
        <input type="hidden" name="id" value="{{ products.id }}">
        {% endif %}
    <div>
        <label for="name">Name:</label>
        <input type="text" name="name" id="name"  />
    </div>
     <div>
        <input type="submit" type="submit" />
    </div>
</form>